# Portable dev tools (Node.js + PostgreSQL binaries and data) — machine-local
tools/node/
tools/pgsql/
tools/pgdata/
tools/pg.log

node_modules/
.env
.env.local
.claude/settings.local.json

# Live data dump (production PII + password hashes) — NEVER commit
live-data.json
backend/live-data.json
